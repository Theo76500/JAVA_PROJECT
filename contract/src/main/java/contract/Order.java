package contract;

/**
 * @author LAGNLOIS Theo  
 * Enumeration of all the order can be send to the controller by the view
 */

public enum Order {
		CHARACTER_UP,
		CHARACTER_DOWN,
		CHARACTER_LEFT,
		CHARACTER_RIGHT,
		RETRY,
}
